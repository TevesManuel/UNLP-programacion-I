program app;

var

    entero: Integer;
    flotante: Real;
    caracter: char;
    booleano: boolean;

begin

    entero := 4;
    flotante := 2.0;
    caracter := 'a';
    booleano := true;

    WriteLn('El valor de la variable de tipo int es ', entero);
    WriteLn('El valor de la variable de tipo float es ', flotante);
    WriteLn('El valor de la variable de tipo char es ', caracter);
    WriteLn('El valor de la variable de tipo boolean es ', booleano);

end.